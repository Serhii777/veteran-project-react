import React from "react";

import styles from "./HeaderTitle.module.css";

const HeaderTitle = () => {
  return (
    <div className={styles.headerTitle}>
      <h1>Hello from HeaderTitle</h1>
    </div>
  );
};

export default HeaderTitle;